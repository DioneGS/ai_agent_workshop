#!/usr/bin/env Rscript

args <- commandArgs(trailingOnly = TRUE)

if (length(args) >= 1 && args[1] == "--version") {
  cat("mytools 0.1.0\n")
  quit(status = 0)
}

if (length(args) == 0) {
  cat("usage: mytools --version\n", file = stderr())
  quit(status = 2)
}
